
const circle ={     //object is JS are defined like this
    radius: 1,  
    location:{   //radius and location are properties of the object circle
        x:1,
        y:1
    },
    draw: function(){        //draw is a method of the object circle 
        console.log('draw')  
    }
};

circle.draw();
